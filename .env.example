# The Go backend (ledgerline-backend), including its version prefix.
# Server-side only — no NEXT_PUBLIC_: every call to the API goes out from a
# Server Component or a Server Action, never from the browser.
LEDGERLINE_API_URL=http://localhost:8080/api/v1
